{
  "name": "rstack-editor-fixture-rstack",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "E2E fixture: an rstack-cli project whose only config is `rstack.config.ts`, which lights the Rstest, rs fmt and (bridged) Rslint stacks.",
  "dependencies": {
    "rstack": "0.6.1"
  },
  "devDependencies": {
    "jiti": "^2.0.0"
  }
}
